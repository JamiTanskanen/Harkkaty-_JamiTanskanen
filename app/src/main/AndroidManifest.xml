<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lutemonapp_jamitanskanen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LutemonApp_JamiTanskanen">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CreateLutemonActivity" />
        <activity android:name=".ListLutemonsActivity" />
        <activity android:name=".MoveLutemonActivity" />
        <activity android:name=".BattleArenaActivity" />
        <activity android:name=".TrainingCenterActivity" />
        <activity android:name=".ReviveLutemonActivity" />

    </application>

</manifest>
